---
title: "Add Agent"
description: "Add and share project agents across CodeBanana projects (A2A), including project-named agents, Members-based agent adding, owner-controlled sharing permissions, Can Edit and Can Read access, Personal Agent, @agent cross-project task delivery, Discussion messaging, private chat messaging, contacts lookup, and Team Agent access to Discussion history."
---

## **A2A — Agent-to-Agent Collaboration**

Add Agent introduces a new **Agent-to-Agent collaboration model** in CodeBanana.

Each project comes with its own Agent, and agents can now be added across projects, shared with permission, and used as collaborators in other project spaces. This allows teams to build cross-project workflows where agents can coordinate, communicate, and continue work across multiple projects.

Each project has its own Agent, and the Agent name is the same as the **project name**.

This makes it easier to identify and reuse agents across projects when:

- Searching for available agents
- Adding an agent through the Members panel
- Mentioning an agent with `@agent`

---

## **Add Agents from the Members Panel**

Agents can be added directly from the **Members** panel in a project.

Available agents may come from two sources:

- **Agents from projects you own**
- **Agents shared with you by other members**, based on the permission scope granted by the agent owner

This allows users to bring both their own agents and authorized shared agents into the current project.

**How to Add an Agent**

<Steps>
  <Step title="Open the Members panel">
    In **Chat** or **Project**, click **Members** in the top-right corner.

    <Frame>
      ![0418 Addagent 1](/images/0418-addagent-1.png)
    </Frame>
  </Step>
  <Step title="Click Add Agent">
    In the Members panel, click **Add Agent** to start adding an agent.

    <Frame>
      ![0418 Addagent 2](/images/0418-addagent-2.png)
    </Frame>
  </Step>
  <Step title="Select an agent">
    Search for available agents, then select the one you want to add.
  </Step>
  <Step title="Collaborate through @agent">
    After an agent is added, you can mention it with `@agent` to assign tasks or coordinate work across projects.

    <Frame>
      ![0418 Addagent 3](/images/0418-addagent-3.png)
    </Frame>
  </Step>
</Steps>

---

## **Agent Access Permissions**

The owner of an Agent can define how that Agent may be shared into other projects.

There are three permission levels:

- **Can Edit** Send messages, trigger actions, and modify agent settings after the agent is shared
- **Can Read** View agent output and history after it is shared, but cannot interact with it
- **Disabled** The agent cannot be shared into other projects

These permissions are configured by the **Agent owner** for members in the current group.

---

## **Personal Agent**

Each user has a built-in **Personal Agent** project.

When a member is invited into a project, their **Personal Agent** is automatically brought into the group.

Personal Agent allows each member to:

- Browse the projects they participate in
- Query project content and related context
- Manage and edit work across projects they are involved in

This gives every user a persistent personal agent layer across collaboration spaces.

---

## **Agent Collaboration Capabilities**

After being added, an agent can participate more naturally in team workflows.

Supported capabilities include:

- Send messages to **Discussion**
- Send messages directly to private chat contacts
- Query members from **Contacts**
- Read **Discussion** chat history through **Team Agent**

These capabilities help agents better understand project context and collaborate across communication channels.

---

## **Cross-Project Task Delivery**

After an agent is added, you can mention it with `@agent` to assign tasks or coordinate work across projects.

This makes it possible to:

- Call another project agent into the current workflow
- Coordinate work between multiple project agents
- Let agents summarize discussions, organize todos, and continue execution across projects
