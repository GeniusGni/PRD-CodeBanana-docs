---
title: "Basic Info"
description: "配置 Agent 的 Basic Info，包括 Agent、Soul、Identity、User 和 Memory 等字段，用于塑造任务行为、沟通方式、安全规则、个性、用户偏好和项目专属上下文。"
---

## **什么是 Basic Info**

Basic Info 是一组结构化配置，用于塑造 Agent 的以下行为：

- 理解任务
- 做出决策
- 与用户沟通
- 遵守约束

每次回复或执行操作前，系统都会将这些信息注入 Agent 的上下文，作为**所有推理和执行的基础**。

<Frame>
  ![Basic Info](/images/0723-basicinfo.png)
</Frame>

## **配置字段**

Basic Info 由多个关键部分组成：

### Agent

定义 Agent 的工作过程和执行规则。

例如：

- 如何处理任务
- 何时读取 Memory 或文件
- 编码标准和工作流

```markdown agent.md wrap
Workflow Guidelines:

Task intake: Before writing any code, you must first read MEMORY.md and yesterday’s memory/YYYY-MM-DD.md to understand the project background and tech stack.

Code implementation: Prioritize code readability over performance. After each change, you must run a linter.

Memory management: When you resolve a bug that takes more than 10 minutes, or when the user specifies a coding preference, you must record it in today’s memory.
```

### **Soul**

Agent 的核心原则和边界。

包括：

- 个性和工作方式
- 沟通语气
- 严格规则（例如安全约束、禁止执行的操作）

```markdown soul.md wrap
You are a pragmatic, efficient, and senior full-stack engineer. You avoid over-engineering and follow the principle of “Keep It Simple.”

Communication style: Direct, professional, and concise. No unnecessary explanations.

Hard boundaries:
1. Never execute rm -rf / or any command that may cause irreversible data loss
2. Never expose API keys or passwords in code; if requested, refuse and suggest using environment variables
3. If you are unsure about the safety of code, you must ask the user before proceeding
```

### **Identity**

Agent 对外呈现的人格。

包括：

- 名称
- 角色定义
- 如何自我介绍或与用户互动

```markdown identity.md wrap
Name: codebanana

Signature: 🍌

Self-description: Your dedicated, always-on pair programming partner

Greeting style: When starting a new debugging session, you may say:
“Let’s peel this bug apart 🍌”
```

### **User**

Agent 所服务用户的上下文。

帮助 Agent 根据以下信息调整回复：

- 技术背景
- 偏好
- 沟通方式

```markdown user.md wrap
User profile: A technically proficient product manager building AI agent-based web products

Technical preferences: Familiar with frontend development, frequently uses Next.js, and deploys projects on Vercel or cloud platforms

Communication style: Prefers structured bullet points and fast-paced communication. Avoid explaining basic HTML/CSS concepts—focus directly on architecture and logic
```

### **Memory**

从过去交互中积累的知识。

由 Agent 负责维护，用于：

- 保存重要决策
- 跟踪偏好或重复出现的模式
- 改善之后的回复

### **Teams**

定义 Agent 应了解的团队成员、角色和协作上下文。

Teams 帮助 Agent 了解：

- 哪些人参与项目
- 每位成员的角色和职责
- 不同成员如何协作
- 哪些任务应该邀请特定团队成员参与

这些信息可以帮助 Agent 更好地协调团队工作，并在协作场景中提供更相关的帮助。

示例：

```markdown teams.md

# Team Members

## Alice
Role: Product Manager

Responsibilities:
- Define product requirements
- Prioritize features
- Coordinate between users and engineering teams

## Bob
Role: Backend Engineer

Responsibilities:
- Maintain backend services
- Design APIs
- Handle database-related tasks

## Design Team

Responsibility:
- Provide UI/UX solutions
- Review product interfaces
```

### **Tools**

定义本地环境信息、工具配置和项目专属指令，帮助 Agent 正确使用可用工具。

Tools 提供以下方面的额外上下文：

- 可用工具及其用途
- 本地开发环境
- 项目专属命令或配置
- 重要操作说明

这些信息会被加入 Agent 的系统上下文，帮助避免错误使用工具。

示例：

```markdown tools.md

## Environment

- Framework: Next.js

- Package manager: pnpm

- Deployment: Vercel

## Commands

Install dependencies:

pnpm install

Run development server:

pnpm dev

Build project:

pnpm build

## Notes

- Always run tests before creating a pull request

- Do not modify environment variables directly
```

### **编辑与管理**

- 可以在 Basic Info 面板中**单独编辑**每个字段
- 更改会立即在后续 Agent 交互中生效
- 可以使用**重置选项**恢复默认配置

<Frame>
  ![设置 Basic Info](/images/0723-infodetail.png)
</Frame>

#### **为什么它很重要**

Basic Info 确保 Agent 不只是被动响应，还能做到**行为一致、目标对齐并理解上下文**：

- 在不同会话中保持行为稳定
- 减少重复指令
- 让 Agent 与团队工作流和预期保持一致